<UserControl x:Class="Willowcat.CharacterGenerator.UI.View.ChartHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Willowcat.CharacterGenerator.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0"
                Content="🡰" 
                Command="{Binding GoToPreviousCommand}" 
                Margin="5" 
                Padding="5 0"/>
        <Button Grid.Column="2"
                Content="🡲" 
                Command="{Binding GoToNextCommand}" 
                Margin="5" 
                Padding="5 0"/>
    </Grid>
</UserControl>
